\section{WorldEpisode Architecture}
\label{sec:architecture}

WorldEpisode is implemented as a sidecar blueprint around existing formats. It does not ask a
LeRobot dataset to become a USD scene, or a USD scene to become a policy-training table. Instead,
it records the relationships those containers need in order to agree about the same episode: the
world revision, persistent entities, space/time conventions, action contract, asset roles,
interaction events, provenance, and split lineage.

The sidecar is not a mandate to package a global robot corpus as millions of small folders. The
single-episode record is the semantic unit. Production datasets add a manifest layer: globally
scoped namespaces, URI resolvers, shard catalogs, materialized indexes, split manifests, and
append-only release snapshots. Opening such a corpus starts from the manifest and its indexes; it
does not require recursively scanning an object store or loading every episode.

Figure~\ref{fig:worldepisode-bridge} shows the intended data flow. Dataset and log containers plug
in on the left; world assets and replay targets plug in on the right; the WorldEpisode sidecar
holds the semantics that would otherwise be scattered across undocumented loader assumptions.

\begin{figure}[t]
\centering
\resizebox{0.98\linewidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
  \node[wecard=weBlue, font=\sffamily\footnotesize, text width=2.55cm, minimum height=0.92cm] (lerobot) at (0,1.8) {\textbf{LeRobot v3}\\state/action/video};
  \node[wecard=weCyan, font=\sffamily\footnotesize, text width=2.55cm, minimum height=0.92cm] (rerun) at (0,0.45) {\textbf{Rerun / MCAP}\\multirate logs};
  \node[wecard=weViolet, font=\sffamily\footnotesize, text width=2.55cm, minimum height=0.92cm] (ncore) at (0,-0.9) {\textbf{NCore}\\sensor calibration};

  \node[wecard=weInk, fill=wePanel, text width=4.55cm, minimum height=3.65cm] (core) at (5.0,0.45) {};
  \node[font=\sffamily\bfseries\normalsize, text=weInk] at (core.north) [yshift=-0.32cm] {WorldEpisode};
  \node[font=\sffamily\scriptsize, text=weSlate] at (5.0,1.33) {sidecar blueprint};
  \node[wechip=weBlue, font=\sffamily\tiny, text width=1.15cm] at (3.85,0.65) {identity};
  \node[wechip=weCyan, font=\sffamily\tiny, text width=1.55cm] at (6.05,0.65) {frames + clocks};
  \node[wechip=weAmber, font=\sffamily\tiny, text width=1.55cm] at (3.85,-0.05) {action contract};
  \node[wechip=weGreen, font=\sffamily\tiny, text width=1.45cm] at (6.05,-0.05) {roles + events};
  \node[wechip=weRose, font=\sffamily\tiny, text width=2.2cm] at (4.95,-0.75) {provenance + lineage};

  \node[wecard=weGreen, font=\sffamily\footnotesize, text width=2.55cm, minimum height=0.92cm] (usd) at (10.0,1.8) {\textbf{OpenUSD}\\world composition};
  \node[wecard=weAmber, font=\sffamily\footnotesize, text width=2.55cm, minimum height=0.92cm] (gltf) at (10.0,0.45) {\textbf{glTF / SPZ}\\Gaussian splats};
  \node[wecard=weRose, font=\sffamily\footnotesize, text width=2.55cm, minimum height=0.92cm] (sim) at (10.0,-0.9) {\textbf{MuJoCo / Isaac}\\replay targets};

  \draw[wearrow=weBlue] (lerobot.east) -- ($(core.west)+(0,1.0)$);
  \draw[wearrow=weCyan] (rerun.east) -- (core.west);
  \draw[wearrow=weViolet] (ncore.east) -- ($(core.west)+(0,-1.0)$);
  \draw[wearrow=weGreen] ($(core.east)+(0,1.0)$) -- (usd.west);
  \draw[wearrow=weAmber] (core.east) -- (gltf.west);
  \draw[wearrow=weRose] ($(core.east)+(0,-1.0)$) -- (sim.west);

  \node[wechip=weInk, fill=wePanel, font=\sffamily\tiny, text width=10.7cm, minimum height=0.43cm, align=center] at (5,-2.05)
    {validator diagnostics \(\cdot\) conversion-loss reports \(\cdot\) lineage-safe splits \(\cdot\) replay assumptions};
\end{tikzpicture}%
}
\caption{WorldEpisode is the semantic bridge between robot-learning containers, reconstructed
world assets, and simulator targets. It preserves the world--episode contract without replacing
the storage formats on either side.}
\label{fig:worldepisode-bridge}
\end{figure}

This architecture gives the paper its central rule: keep native containers for the data they are
good at storing, but make the cross-container assumptions explicit, checkable, and loss-aware.
