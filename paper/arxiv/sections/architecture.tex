\section{The Sidecar Contract}
\label{sec:contract}

WorldEpisode is implemented as a sidecar blueprint around existing formats. It does not ask a LeRobot
dataset to become a USD scene, a game-engine telemetry log to become an AV replay format, or a USD
scene to become a policy-training table. Instead, it records the relationships those containers
need in order to agree about the same state: the state revision, persistent entities, space/time
conventions, transition contract, asset roles, interaction events, provenance, and split lineage.
\takeaway{WorldEpisode records the cross-container relationships that loaders otherwise leave
implicit; it does not force any format to become another.}

The sidecar is not a mandate to package a global robot corpus as millions of small folders. The
single-episode record is the semantic unit. Production datasets add a manifest layer: globally
scoped namespaces, URI resolvers, shard catalogs, materialized indexes, split manifests, and
append-only release snapshots. Opening such a corpus starts from the manifest and its indexes; it
does not require recursively scanning an object store or loading every episode.

Figure~\ref{fig:worldepisode-bridge} shows the intended data flow. Dataset, log, and telemetry
containers plug in on the left; world assets and replay targets plug in on the right; the
WorldEpisode sidecar holds the semantics that would otherwise be scattered across undocumented
loader assumptions.

\begin{figure}[t]
\centering
\resizebox{0.98\linewidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
  \node[wecard=weBlue, font=\sffamily\scriptsize, text width=2.65cm, minimum height=0.92cm] (lerobot) at (0,1.8) {\textbf{LeRobot v3}\\state/action/video};
  \node[wecard=weCyan, font=\sffamily\scriptsize, text width=2.65cm, minimum height=0.92cm] (rerun) at (0,0.45) {\textbf{Rerun / MCAP}\\multirate logs};
  \node[wecard=weViolet, font=\sffamily\scriptsize, text width=2.65cm, minimum height=0.92cm] (ncore) at (0,-0.9) {\textbf{Game / AV logs}\\telemetry streams};

  \node[wecard=weInk, fill=wePanel, text width=4.75cm, minimum height=3.65cm] (core) at (5.0,0.45) {};
  \node[font=\sffamily\bfseries\small, text=weInk, align=center] at (core.north) [yshift=-0.34cm] {WorldEpisode};
  \node[font=\sffamily\tiny, text=weSlate] at (5.0,1.29) {state-integrity sidecar};
  \node[wechip=weBlue, font=\sffamily\tiny, text width=1.25cm] at (3.85,0.65) {identity};
  \node[wechip=weCyan, font=\sffamily\tiny, text width=1.45cm] at (6.08,0.65) {space + time};
  \node[wechip=weAmber, font=\sffamily\tiny, text width=1.75cm] at (3.82,-0.05) {transition rules};
  \node[wechip=weGreen, font=\sffamily\tiny, text width=1.50cm] at (6.08,-0.05) {roles + events};
  \node[wechip=weRose, font=\sffamily\tiny, text width=2.25cm] at (4.95,-0.75) {provenance + lineage};

  \node[wecard=weGreen, font=\sffamily\scriptsize, text width=2.65cm, minimum height=0.92cm] (usd) at (10.0,1.8) {\textbf{OpenUSD}\\world composition};
  \node[wecard=weAmber, font=\sffamily\scriptsize, text width=2.65cm, minimum height=0.92cm] (gltf) at (10.0,0.45) {\textbf{glTF / SPZ}\\Gaussian splats};
  \node[wecard=weRose, font=\sffamily\scriptsize, text width=2.65cm, minimum height=0.92cm] (sim) at (10.0,-0.9) {\textbf{Runtime adapters}\\replay targets};

  \draw[wearrow=weBlue] (lerobot.east) -- ($(core.west)+(0,1.0)$);
  \draw[wearrow=weCyan] (rerun.east) -- (core.west);
  \draw[wearrow=weViolet] (ncore.east) -- ($(core.west)+(0,-1.0)$);
  \draw[wearrow=weGreen] ($(core.east)+(0,1.0)$) -- (usd.west);
  \draw[wearrow=weAmber] (core.east) -- (gltf.west);
  \draw[wearrow=weRose] ($(core.east)+(0,-1.0)$) -- (sim.west);

  \node[wechip=weInk, fill=wePanel, font=\sffamily\tiny, text width=10.7cm, minimum height=0.43cm, align=center] at (5,-2.05)
    {validator diagnostics \(\cdot\) conversion-loss reports \(\cdot\) lineage-safe splits \(\cdot\) replay assumptions};
\end{tikzpicture}%
}
\caption{WorldEpisode is the semantic bridge between spatial data containers, state/world assets, and
runtime targets.}
\label{fig:worldepisode-bridge}
\end{figure}

This architecture gives the paper its central rule: keep native containers for the data they are
good at storing, but make the cross-container assumptions explicit, checkable, and loss-aware.

\subsection{WorldEpisode as a Meta-Simulator Contract}

WorldEpisode is not tied to a single rendering or physics engine. It operates as a
meta-simulator contract: a runtime becomes a trusted target by implementing an adapter that
preserves the same semantic invariants and emits replay evidence. This keeps the integration
direction clean. Datasets are not rewritten around simulator-native assumptions; simulators compile
their scene loading, action interfaces, and replay reports against the WorldEpisode sidecar.

The adapter contract has three layers:
\begin{itemize}[leftmargin=1.4em,itemsep=2pt,topsep=3pt]
  \item \textbf{Invariant interface}: ingests immutable world revisions, frame and clock graphs,
  entity identity, representation roles, action channels, asset digests, provenance, and quality
  records.
  \item \textbf{Asynchronous schema extension}: lets an engine register cloth, deformable, fluid,
  articulation, material, or procedural-scene extensions without weakening the core episode record.
  \item \textbf{Deterministic replay accountability}: records runtime identity, version, solver,
  timestep, actuator parameters, initialization state, command/effective timing, latency, tolerance
  envelopes, and measured drift.
\end{itemize}

The current implementation reflects this boundary across two codebases: WorldEpisode has tested
minimal MuJoCo~\citep{mujoco} and Genesis replay adapters for the same LeRobot control-loop trace,
and URDF Studio, the companion simulator-transfer workbench, passes a shared \texttt{SimBackend}
conformance suite for both engines with a one-episode MuJoCo--Genesis comparison report. The Isaac
mapping is emitted but untested, and SAPIEN remains adapter-required. The claim is therefore not
simulator-independent physics but runtime-neutral accountability: every simulator reports against
the same state contract, so cross-runtime drift becomes measurable rather than hidden inside loader
conventions.
