\section{The Five Invariants}
\label{sec:invariants}

Figure~\ref{fig:five-graph-model} shows the five coupled graphs that make a spatial state
replayable and auditable rather than just stored. Each graph answers a question that appears during
debugging: what object is this, where and when is it, what is the asset safe to do, what physically
or virtually happened, and where did this data come from?
\takeaway{Five graphs answer five debugging questions and are made mutually referential, so an
identity, timing, role, event, or lineage error becomes detectable instead of hidden.}

\begin{figure}[t]
\centering
\resizebox{0.90\linewidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
  \node[wecard=weInk, fill=wePanel, font=\sffamily\scriptsize, text width=4.05cm, minimum height=1.55cm] (center) at (0,0)
    {\textbf{WorldEpisode}\\USS robotics profile\\[2pt]
     state revision\\+ ordered deltas};

  \node[wecard=weBlue, font=\sffamily\scriptsize, text width=2.75cm, minimum height=1.0cm] (identity) at (0,2.45)
    {\textbf{Identity}\\persistent entities};
  \node[wecard=weCyan, font=\sffamily\scriptsize, text width=2.95cm, minimum height=1.0cm] (frame) at (4.0,0.85)
    {\textbf{Frame + Clock}\\transforms, timing};
  \node[wecard=weAmber, font=\sffamily\scriptsize, text width=2.85cm, minimum height=1.0cm] (role) at (2.05,-2.15)
    {\textbf{State Roles}\\appearance, physics};
  \node[wecard=weGreen, font=\sffamily\scriptsize, text width=2.85cm, minimum height=1.0cm] (event) at (-2.05,-2.15)
    {\textbf{Temporal Events}\\contacts, patches};
  \node[wecard=weRose, font=\sffamily\scriptsize, text width=2.95cm, minimum height=1.0cm] (prov) at (-4.0,0.85)
    {\textbf{Provenance}\\lineage, uncertainty};

  \draw[wearrow=weBlue] (identity.south) -- (center.north);
  \draw[wearrow=weCyan] (frame.west) -- (center.east);
  \draw[wearrow=weAmber] (role.north west) -- (center.south east);
  \draw[wearrow=weGreen] (event.north east) -- (center.south west);
  \draw[wearrow=weRose] (prov.east) -- (center.west);

  \draw[very thick, draw=weLine] (identity) -- (frame) -- (role) -- (event) -- (prov) -- (identity);
  \node[wechip=weViolet, fill=weViolet!10, font=\sffamily\tiny, text width=6.4cm, minimum height=0.36cm] at (0,-3.35)
    {closed references + digest-verified assets + explicit loss reports};
\end{tikzpicture}%
}
\caption{The five-graph model. WorldEpisode makes identity, frames/clocks, state roles, temporal events, and
provenance mutually referential so conversion, synchronization, and replay errors become detectable
instead of hidden.}
\label{fig:five-graph-model}
\end{figure}

\subsection{Identity graph: what object is this? (\texttt{ENTITY.001})}

Every physical or logical entity --- robot, link, gripper, sensor, rigid or articulated object,
fixture, region, human --- receives a persistent identifier, and the same \texttt{entity\_id} must
be used by segmentation masks, Gaussian groups, render meshes, collision bodies, simulator actors,
language references, and interaction events. The real-world fix is simple: if a language command
says ``pick up the mug,'' the perception mask, the simulator object, and the collision proxy must
identify the same mug.

\subsection{Frame and clock graph: where is it, and exactly when? (\texttt{FRAME.001}, \texttt{TIME.001})}

Every spatial value declares source frame, target frame, units, handedness, up axis, transform
direction, quaternion convention, validity interval, calibration revision, and uncertainty where
available, consistent with and extending ROS's coordinate and unit conventions~\citep{rosrep103}. Every stream declares a clock domain, device timestamp, host-receive timestamp where
available, exposure interval for cameras, effective command time for actions, synchronization
mapping, and estimated offset/drift/error. NCore's explicit pose-graph convention demonstrates the
kind of rigor WorldEpisode requires across the whole state record~\citep{ncore}. This graph is what turns a
suspicious replay from ``the policy is bad'' or ``the engine is wrong'' into a diagnosable offset,
transform direction, or unit error.

\subsection{Representation-role graph: what is this asset safe for? (\texttt{REP.001})}

One physical or virtual entity can have many representations: Gaussian splat appearance, textured
render mesh, metric surface mesh, depth point cloud, collision proxy, mass/inertia record, semantic
affordance, telemetry stream, or learned embedding. Each representation declares its role and an
asset descriptor: URI, media type, SHA-256 digest, optional mirrors, optional embedded payload
metadata, coordinate frame, units, validity interval, derivation, uncertainty, and license. The URI
may be relative, HTTP(S), Hugging Face, object storage, OCI, IPFS, or another registered resolver.
Portability comes from deterministic resolution and digest verification, not from requiring every
asset to live in one folder. The role field is essential: a splat may be valid for appearance but
invalid for collision, while a simplified proxy may be valid for collision but unsuitable for visual
training.

\subsection{Temporal-event graph: what actually happened? (\texttt{TRACE.001})}

Spatial records do not store only poses. They record interaction events --- contact begin/end,
grasp establish/release, attachment, map or collision patch, articulation change, spawn/despawn,
simulator reset, intervention, subgoal completion, failure detection --- each referencing entities,
time, confidence, source, and optionally evidence. This turns raw coordinate streams into
milestones such as ``contact established,'' ``object dropped,'' or ``intervention occurred.''

\subsection{Provenance graph: where did this come from? (\texttt{PROV.001}, \texttt{SPLIT.001})}

Every derived asset identifies its source episodes, sensors and intervals, reconstruction algorithm
and version, model/checkpoint hash, calibration revision, source and output hashes, creator,
license, and quality metrics, mapping to existing provenance vocabularies rather than inventing an
incompatible one. This graph lets a split avoid putting the same reconstructed room, capture
session, or generated asset family on both sides of evaluation.
